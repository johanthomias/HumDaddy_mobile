# ===========================================
# HumDaddy Mobile - Variables d'environnement
# ===========================================
#
# Copiez ce fichier en `.env` et modifiez les valeurs.
#
# IMPORTANT - Pour device physique (iPhone/Android avec Expo Go):
# - localhost ne fonctionne PAS depuis un device physique
# - Utilisez l'IP locale de votre Mac/PC
#
# Comment trouver votre IP locale :
# - macOS/Linux : ifconfig | grep "inet " | grep -v 127.0.0.1
# - Windows     : ipconfig (cherchez "IPv4 Address")
#
# Pour vérifier que le backend est accessible depuis le device :
# - Ouvrez Safari/Chrome sur le device
# - Allez sur http://VOTRE_IP:4000/health
# - Vous devez voir une réponse JSON
#
# Après modification du .env :
# - Arrêtez le bundler Expo (Ctrl+C)
# - Relancez avec : npx expo start -c
#   (le -c force un clear du cache)
#
# ===========================================

# URL de l'API backend
# - Simulateur : http://localhost:4000
# - Device physique : http://192.168.x.x:4000 (remplacez par votre IP)
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000
